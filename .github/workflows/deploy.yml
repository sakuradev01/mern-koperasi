name: Deploy MERN Koperasi to AWS EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'

      - name: Install root dependencies
        run: npm install

      - name: Install client dependencies
        run: npm install
        working-directory: ./client
        
      - name: Build React Frontend
        run: npm run build
        working-directory: ./client
      
      # Step 1: Menyalin file ke EC2
      - name: Copy files to EC2
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "./"  # Salin semua file dari root repo
          target: "/home/${{ secrets.EC2_USER }}/app-staging/" # ke folder staging di EC2

      # Step 2: Menjalankan skrip deployment di EC2
      - name: Run deployment scripts on EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # Menghentikan layanan yang sedang berjalan
            pm2 stop mern-koperasi-backend || true
            pm2 delete mern-koperasi-backend || true
            
            # Hapus konten lama di Nginx
            sudo rm -rf /var/www/html/*
            
            # Salin file baru dari staging ke tempat yang benar
            sudo cp -r /home/${{ secrets.EC2_USER }}/app-staging/client/dist/. /var/www/html/
            
            # Mengganti URL server di konfigurasi frontend
            # Menggunakan skrip yang lebih aman untuk mencegah error jika file tidak ada
            if [ -f "/var/www/html/assets/conf.js" ]; then
              sudo sed -i "s|VITE_SERVER_URL=\"http://localhost:5173\"|VITE_SERVER_URL=\"http://${{ secrets.EC2_HOST }}:5000\"|g" /var/www/html/assets/conf.js
            else
              echo "Warning: /var/www/html/assets/conf.js not found. Skipping sed command."
            fi
            
            # Menjalankan backend
            cd /home/${{ secrets.EC2_USER }}/app-staging/server
            pm2 start index.js --name "mern-koperasi-backend"
            
            # Menyimpan konfigurasi PM2
            pm2 save --force
            
            # Membersihkan folder staging
            sudo rm -rf /home/${{ secrets.EC2_USER }}/app-staging
